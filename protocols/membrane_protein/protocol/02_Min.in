Thorough minimization w/ restraints on protein heavy atoms
  &cntrl
    !General
      imin=1,
    !Input
    !Output
      ntpr=50,
    !Restraints
      ntr=1,
      restraint_wt=100.0,
      restraintmask='(PROTEIN_MASK) | (LIGAND_MASK)',
    !Minimization
      maxcyc=10000,
      ncyc=5000,
    !MD
    !Temperature
    !Pressure
    !SHAKE
    !Potential
      cut=10.0,
  /
